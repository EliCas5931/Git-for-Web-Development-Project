Codepen URL:
https://codepen.io/Elicas5931/pen/OJNOBOg?editors=1100

<!-- Questions to Submit in today's file first-lastname.txt --> 
<!--
Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? 
        Semantic elements are elements that clearly define its content in HTML. Examples include table, article, address, etc.
    
    2. What is HTML used for? 
        HTML is used as the structure and basic building blocks of webpages and stands for Hyper Text Markup Language.

    3. What is an attribute and where do we put it? 
        An attribute is used to provide additional information about HTML elements and is always in the start tag. Some common attributes include href and src.

    4. What is the h1 tag used for? How many times should I use it on a page?
        The H1 tag is used to signify an important title and should only be used once on a page.

    5. Name two tags that have required attributes
        Two tags that have required attributes are the <img> and <a> tags.

    6. What do we put in the head of our HTML document? 
        We put the title element in the head section of the HTML document.

    7. What is an id? 
        An id is a selector that uses the id attribute of an element to select a specific element. 

    8. What elements can I add an id to? 
        You can add an id to any element but you can not have more than one element with the same id. 

    9. How many times can I use the same id on a page? 
        You can only use it once per page. 

    10. What is a class? 
        Class is an attribute to specify a class for an HTML element. 

    11. What elements can I add a class to? 
        You can add a class to as many elements as you want. 

    12. How many times can I use the same class on a page? 
        You can use them as many times as long as they're appropriate and fits.

    13. How do I get my link to open in a new tab?
        You can add target="_blank" to an anchor tag.

    14. What is the alt attribute in the image tag used for? 
        If an image doesn't load properly or if a user has accessability turned on, the alt attribute will show what the image is meant to be.

    15. How do I reference an id?
         You can reference an id by using the # character.

    16. What is the difference between a section and a div
        A section defines sections in a document like headers or footers and a div tag defines a division or a section in an HTML document.
    
    17. What is CSS used for? 
        CSS is used to style webpages.

    18. How to we select an element? Example - every h2 on the page
        We can select an element by referencing the element itself. such as
            h2 {

            }

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
        A class and an id are used as selectors. Classes can be used multiple times and an id should only be used once.

    20. How do we select classes in CSS?
        You can select classes by use the . before the name of the class.

    21. How do we select a p element with a single class of “human””?
        You can select it by using p.human

    22. What is a parent child selector? When would this be useful? 
        It selects all elements that are a direct child of that specific element. It's useful to give relation between two elements.

    23. How do you select all links within a div with the class of sidebar?
        You can select it by using 
        
        div.sidebar a {

        }

    24. What is a pseudo selector?
        It's a selector that selects elements that are in a speicific state, like the first of their type, or being hovered over by the mouse.

    25. What do we use the change the spacing between lines?
        You can use line-height to change the spacing between lines.

    26. What do we use to change the spacing between letters?
        You can use letter-spacing to change the spacing between letters.

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
        You can use text-transform

    28. How do I add a 1px border around my div that is dotted and black?
        You can add it by div {
            border-style: 1px black dotted;
        }

    29. How do I select everything on the page? 
        You can select everything on the page by using the body tag. 

    30. How do I write a comment in CSS?
        You can write a comment in CSS by using the command + / 
    
    31. How do I find out what file I am in, when I am using the command line? 
        You can type in pwd in the command line to figure out what working directory you're in. 

    32. Using the command line - how do I see a list of files/folders in my current folder?
        You can type ls to see a list of files/folders

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
        You can remove a file by rm example.file 
        You have to be careful using this because you can remove the wrong things and mess up the directory.

    34. Why should I use version control? 
        You can use version control to track changes, especially when working in groups. It helps members collaborate without destorying or messing up the main/original code

    35. How often should I commit to github?
        You should commit as often as possible, when adding new code, to ensure you don't lose any code

    36. What is the command we would use to push our repo up to github? 
        git push origin (branch)

    37. Walk me through Lambda's git flow. 
        Fork the project
	Add TL as collaborator
	Navigate to the location on your computer in command prompt/ terminal
	Clone repo and cd into that file
	Create branch for work
	Code!
	Add, commit and push as you make changes
	TL will review your code and accept pull request 
	DO NOT MERGE your own pull request


Stretch Questions

    1. What is the difference between an inline element and a block element?
	Block elements will take up a whole line where as inline does not move to a	a new line

    2. What happens when an element is positioned absolutely? 
	Absolute will position relative to parent

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
	
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 



   --> 